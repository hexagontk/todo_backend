
apiVersion: apps/v1
kind: Deployment

metadata:
  name: mongodb
  namespace: todo
  labels:
    io.kompose.service: mongodb
  annotations:
    environment: local

spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: mongodb
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        io.kompose.service: mongodb
      annotations:
        environment: local
    spec:
      containers:
        - image: docker.io/mongo:7-jammy
          name: mongodb
          ports:
            - containerPort: 27017
          volumeMounts:
            - mountPath: /data/db
              name: mongodb
      restartPolicy: Always
      volumes:
        - name: mongodb
          persistentVolumeClaim:
            claimName: mongodb

---

apiVersion: v1
kind: Service

metadata:
  name: mongodb
  namespace: todo
  labels:
    io.kompose.service: mongodb
  annotations:
    environment: local

spec:
  type: NodePort
  ports:
    - port: 27017
      nodePort: 31000
  selector:
    io.kompose.service: mongodb
