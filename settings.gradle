
rootProject.name = "hextodo"

gradle.rootProject {

    allprojects {
        version = "0.1.0"
        group = "com.hexagonkt"
        description = "Task Manager"
    }
}

include("backend", "frontend")
